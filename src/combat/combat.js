/**
 * Bow, arrows, damage. STUB: gauntlet builder implements draw/loose arrows,
 * projectile physics, part damage, hit FX.
 */
export class Combat {
  constructor(ctx) {
    this.ctx = ctx;
  }
  update(dt, t) {}
}
