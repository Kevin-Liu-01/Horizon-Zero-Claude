/**
 * Procedural WebAudio soundscape. STUB: gauntlet builder implements wind,
 * footsteps, bow, machine chirps, combat music layers.
 */
export class GameAudio {
  constructor(ctx) {
    this.ctx = ctx;
  }
  update(dt, t) {}
}
