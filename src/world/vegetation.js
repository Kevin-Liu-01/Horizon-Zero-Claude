/**
 * Vegetation. STUB: the gauntlet builder replaces this with instanced,
 * wind-swaying tall grass, pines, and rocks.
 */
export class Vegetation {
  constructor(ctx) {
    this.ctx = ctx;
  }
  update(dt, t) {}
}
