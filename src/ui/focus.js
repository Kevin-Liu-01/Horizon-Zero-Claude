/**
 * Focus scan mode. STUB: gauntlet builder implements the pulse, highlight
 * materials, and patrol path visualization.
 */
export class FocusSystem {
  constructor(ctx) {
    this.ctx = ctx;
  }
  update(dt, t) {}
}
