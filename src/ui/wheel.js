/**
 * Weapon wheel. STUB: round-2 builder replaces with the radial hold-to-open
 * wheel (time slow, weapon + ammo selection, craft buttons).
 */
export class WeaponWheel {
  constructor(ctx) {
    this.ctx = ctx;
    this.open = false;
  }
  update(dt, t) {}
}
